apiVersion: v1
kind: Service
metadata:
  name: yujtjndrh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yujtjndrh"
    port: 8080
  selector:
    app: yujtjndrh
